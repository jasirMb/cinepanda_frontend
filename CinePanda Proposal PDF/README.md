
  # Convert PDF to HTML

  This is a code bundle for Convert PDF to HTML. The original project is available at https://www.figma.com/design/fRv5FaIockxd3UdhJOhCvY/Convert-PDF-to-HTML.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  